import { ApiProperty } from '@nestjs/swagger';

export class VoteAlternativeDto {
  // If we need to send anything in the body in the future.
  // For now, it might be empty or we can move alternativeId here if preferred.
}
